import 'package:flutter/material.dart';

class StyleGuide {
  static const primaryColor = Colors.black;
  static const buttonHorSpacing = 75.0;
  static const buttonVertSpacing = 15.0;
  static const buttonVertPadding = 16.0;
  static const logoFontSize = 72.0;
  static const logoIconSize = 85.0;
  static final separator = (context) => MediaQuery.of(context).size.height / 10;
}
